// Re-export the domain types for the products feature.
// These come from Lab 3's types/forms.ts, which you already have at
// the root of your project (@/types/forms).
export type { CreateProductInput, ProductSummary } from '@/types/forms';
export type { Product } from '@/types/index';
